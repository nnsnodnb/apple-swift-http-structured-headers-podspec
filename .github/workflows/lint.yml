name: Lint

on:
  push:
    paths:
    - "swift-http-structured-headers.podspec"
    - ".github/workflows/lint.yml"

jobs:
  pod_lint:
    runs-on: macos-latest
    timeout-minutes: 10

    steps:
    - uses: actions/checkout@v2

    - name: Setup ruby
      uses: ruby/setup-ruby@v1
      with:
        ruby-version: 2.7
        bundler-cache: true

    - name: Lint
      run: |
        gem install cocoapods
        pod lib lint StructuredHeaders.podspec
        pod lib lint CodableStructuredHeaders.podspec

  actionlint:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - uses: reviewdog/action-actionlint@v1
      with:
        reporter: github-pr-review
        level: warning
